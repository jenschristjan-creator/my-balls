{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug (LLDB + Clang)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}\\main.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build"
        }
    ]
}